"""Optional vector-search module (feature-flagged via ``VECTOR_ENABLED``).

Reserved for the Qdrant client and embedding glue. Provider is undecided, so
this package is intentionally a placeholder; see ``app/tasks/embeddings.py``.
"""
